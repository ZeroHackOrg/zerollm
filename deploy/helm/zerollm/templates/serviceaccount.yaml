apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "zerollm.serviceAccountName" . }}
  labels:
    {{- include "zerollm.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- if .Values.imagePullSecrets }}
imagePullSecrets:
  {{- toYaml .Values.imagePullSecrets | nindent 2 }}
{{- end }}